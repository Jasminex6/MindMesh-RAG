# Legacy recovery copies

These files are preserved only because the workspace is not under version control. They are not active implementations and must not be imported, executed, edited, or used as examples for new work.

The canonical entry point is `External/Asthma_RAG_LangChain.ipynb`; canonical code lives in `src/medical_rag/`.
